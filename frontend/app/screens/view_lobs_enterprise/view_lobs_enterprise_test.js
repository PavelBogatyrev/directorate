'use strict';

describe('myApp.view_lobs_enterprise module', function() {

  beforeEach(module('bodApp.view_lobs_enterprise'));

  describe('view_lobs_enterprise controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ViewLobsEntCtrl = $controller('ViewLobsEntCtrl');
      expect(ViewLobsEntCtrl).toBeDefined();
    }));

  });
});