'use strict';

describe('myApp.view_lobs_auto module', function() {

  beforeEach(module('bodApp.view_lobs_auto'));

  describe('view_lobs_auto controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ViewLobsAutoCtrl = $controller('ViewLobsAutoCtrl');
      expect(ViewLobsAutoCtrl).toBeDefined();
    }));

  });
});