'use strict';

describe('myApp.Login module', function() {

  beforeEach(module('bodApp.Login'));

  describe('Login controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var LoginCtrl = $controller('LoginCtrl');
      expect(LoginCtrl).toBeDefined();
    }));

  });
});