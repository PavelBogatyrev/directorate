'use strict';

describe('myApp.view_lobs_global module', function() {

  beforeEach(module('bodApp.view_lobs_global'));

  describe('view_lobs_global controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ViewLobsGlobalCtrl = $controller('ViewLobsGlobalCtrl');
      expect(ViewLobsGlobalCtrl).toBeDefined();
    }));

  });
});