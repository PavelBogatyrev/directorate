'use strict';

describe('bodApp.view_kpi_month module', function() {

  beforeEach(module('bodApp.view_kpi_month'));
  beforeEach(module('bodApp.view_kpi_month'));

  describe('view_kpi_month controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ViewKPIMonthCtrl = $controller('ViewKPIMonthCtrl');
      expect(ViewKPIMonthCtrl).toBeDefined();
    }));

  });
});