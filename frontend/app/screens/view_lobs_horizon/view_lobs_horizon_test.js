'use strict';

describe('myApp.view_lobs_horizon module', function() {

  beforeEach(module('bodApp.view_lobs_horizon'));

  describe('view_lobs_horizon controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ViewLobsHorizonCtrl = $controller('ViewLobsHorizonCtrl');
      expect(ViewLobsHorizonCtrl).toBeDefined();
    }));

  });
});