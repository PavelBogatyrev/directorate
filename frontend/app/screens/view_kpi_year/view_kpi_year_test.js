'use strict';

describe('bodApp.view_kpi_year module', function() {

  beforeEach(module('bodApp.view_kpi_year'));
  beforeEach(module('bodApp.view_kpi_year'));

  describe('view_kpi_year controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ViewKPIYearCtrl = $controller('ViewKPIYearCtrl');
      expect(ViewKPIYearCtrl).toBeDefined();
    }));

  });
});