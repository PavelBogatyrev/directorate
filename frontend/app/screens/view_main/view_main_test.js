'use strict';

describe('myApp.view_main module', function() {

  beforeEach(module('bodApp.view_main'));

  describe('view_main controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewMainCtrl = $controller('ViewMainCtrl');
      expect(viewMainCtrl).toBeDefined();
    }));

  });
});